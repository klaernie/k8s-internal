apiVersion: v1
kind: Service
metadata:
  labels:
    app: calserv
  name: calserv
  namespace: magicmirror
spec:
  ports:
    - name: "80"
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: calserv
  sessionAffinity: None
  type: ClusterIP
