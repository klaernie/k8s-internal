apiVersion: v1
kind: Service
metadata:
  name: abs
  namespace: abs
spec:
  selector:
    name: abs
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  type: ClusterIP
